services:
    _defaults:
        autowire:      true
        autoconfigure: true

    HeimrichHannot\FlareBundle\:
        resource: ../src
        exclude:  ../src/{Contao,ContaoManager,DependencyInjection,DTO,Model}

    HeimrichHannot\FlareBundle\Manager\FilterElementManager:
        arguments:
            $filterElementServices: !tagged_iterator 'huh.flare.filter_element'

#    bind:
#      $projectDir: '%kernel.project_dir%'
#      $csrfTokenName: '%contao.csrf_token_name%'
#      $kernelBundles: '%kernel.bundles%'
